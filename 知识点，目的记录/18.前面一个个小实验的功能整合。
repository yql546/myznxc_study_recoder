
0.
集成前面的实验，嗯。。。手敲一遍？
逻辑和前面的完全一样，
我这里进行移植，不手敲了。


框架：
通过。设置全局变量，作为不同模式的状态标志，
来进入不同模式的处理。

我们，可以通过按键或者蓝牙来改变模式变量。






1.
按键中断，进行消抖处理时，发生了程序卡死异常。

原因：
消抖处理，用到HAL_Delay();延时函数，本质是，通过计数systick中断的
进入次数，来实现指定延时。

按键按下，进入按键的外部中断服务函数。
内部的callback回调函数，调用了延时函数。

此时，外部中断，exti，已经进入，而且，systick的中断优先级
默认是最低的，所以，这个延时函数，根本是无法执行的，卡死在这里。

我们将systick的中断Timer优先级
设置得高于外部中断exti即可。




ok。
到这里，裸机阶段的小车就结束了。

后面的rtos，我会换笔记软件来记录。
这个厂库就不再更新笔记内容了。
笔记软件的笔记的连接后面会发出来